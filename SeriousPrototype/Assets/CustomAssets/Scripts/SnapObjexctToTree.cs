using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnapObjexctToTree : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // DETECT IF A COLLISION IS DETECTED
        // CHECK IF THE COLLISION IS WITH A CORAL
        // CHECK WHAT NODE ON THE TREE, THE COLLISION IS WITH
        // CHECK IF THE CORAL IS RELEASED FROM THE PLAYER'S HAND
        // SNAP THE CORAL TO THE CAPSULE
        // TURN OFF THE COLLISION TRIGER FOR THE PARTICULAR CAPSULE
    }
}
